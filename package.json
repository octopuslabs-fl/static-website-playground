{
  "name": "static-website-playground",
  "version": "1.0.0",
  "description": "A playground for static web experiments, deployed to GCP",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
